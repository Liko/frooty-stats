<div class="container">

    <h1 class="text-center p-5">
        <div class="card p-3">
            <%= @competition.name %>
        </div>
    </h1>

    <%= render "/stats/top_stats"%>

    Teams:
    <br/>
    <% @competition.clubs.each do |club|%>
        <%= image_tag club.image %><br/>
        <%=link_to club.name, club %><br/>
    <% end %>

</div>