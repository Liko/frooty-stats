<h1>Club Listing</h1>
 
<table>
  <thead>
    <tr>
      <th>Club</th>
      <th></th>
      <th>Country</th>
      <th>Founding Year</th>
      <th colspan="3"></th>
    </tr>
  </thead>
 
  <tbody>
    <% @clubs.each do |club| %>
      <tr>
        <td><%= link_to club.name, club %></td>
        <td><%= club.stadium_id %></td>
        <td><%= club.country.name %></td>
        <td><%= club.founded %></td>

        <td><%= link_to "View Club", club %></td>
    
      </tr>
    <% end %>
  </tbody>
</table>
 
<br>

<%= link_to 'Back', url_for(:back) %>